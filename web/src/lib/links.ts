/* Canonical external URLs. Kept in one place so we never scatter invented
   handles across the UI. Only links that actually resolve live here; social
   accounts we don't have yet are intentionally absent. */
export const SITE_URL = 'https://nohrs.app'
export const GITHUB_REPO = 'https://github.com/noh-rs/nohrs'
export const GITHUB_RELEASES = `${GITHUB_REPO}/releases`
export const GITHUB_DISCUSSIONS = `${GITHUB_REPO}/discussions`
export const GITHUB_LICENSE = `${GITHUB_REPO}/blob/develop/LICENSE`
